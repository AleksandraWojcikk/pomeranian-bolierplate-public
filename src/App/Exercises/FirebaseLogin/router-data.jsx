import React from 'react';

import { FirebaseLogin } from './FirebaseLogin';

export const fireBaseLoginRouterMetaData = {
  path: 'firebase-login',
  linkLabel: 'Firebase - logowanie',
  element: <FirebaseLogin />,
};
